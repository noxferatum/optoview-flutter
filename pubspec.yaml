flutter:
  uses-material-design: true
  assets:
    - assets/images/smile.png
  generate: true

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_gen_runner: any
  build_runner: any
